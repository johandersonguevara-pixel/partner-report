# Local: deixe vazio — o Vite faz proxy de /api → localhost:3001
#
# Produção (frontend e API em hosts diferentes, ex. Railway):
# URL pública do backend, SEM barra no fim, ex. https://partner-api.up.railway.app
# Tem de existir no momento do `npm run build` (variável no Railway → Build).
VITE_API_URL=
